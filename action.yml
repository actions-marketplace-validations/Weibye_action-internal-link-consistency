name: 'example_checker'
description: 'Action to cross reference examples in source code, README.md and Cargo.toml to ensure they contain the same info.'
author: 'Andreas Weibye'
inputs:
  source:
    required: true
    description: 'path to source to be scanned'
    default: 'examples/'
  targets:
    required: true
    description: 'Some description'
    default: '{["Cargo.toml", "examples/README.md"]}'
runs:
  using: 'node12'
  main: 'dist/index.js'